/**
 * Created by Shiyu on 4/9/2016.
 */
/*
import React, { Component, PropTypes } from 'react';
import { connect } from 'react-redux'
import { bindActionCreators } from 'redux'
import { fetchOrg } from '../../actions/orgs'
import { push as pushRoute } from 'react-router-redux';
import ProfileChart from '../../components/Organization/ProfileChart'

class ProfileChartContainer extends Component {
    componentDidMount() {
        this.props.dispatch(
            fetchChart(this.props.chart)
        );
    }

    render() {
        return (
            <ProfileChart
                data={this.props.line}
                data2={this.props.pie}/>
        )
    }

}

ProfileChartContainer.propTypes = {
    data: propTypes.array,
    data2: propTypes.array
};

const mapStateToProps = (state) => (
{
    chart: state.chart
}
);

export default connect(mapStateToProps)(ProfileChartContainer)*/
