// add paths from route not relative
import React from 'react';
import ReactDOM from 'react-dom';
import UserRegisterForm from '../../../app/containers/UserRegisterFormContainer';
import { assert }  from "chai";
import TestUtils from "react-addons-test-utils"


describe('UserRegisterForm Component', () => {
    // To test container must wrap in mock store
    // To test componenet must render with necessary props
    // const renderedComponent = TestUtils.renderIntoDocument(
    //     <UserRegisterForm />
    // );
    //
    // const dom = ReactDOM.findDOMNode(renderedComponent);
    //
    //
    // it('it renders correctly', () => {
    //     return assert.isOk(renderedComponent);
    // });
});
